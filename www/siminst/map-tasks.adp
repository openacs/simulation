<if @missing_props_p@>

  <div class="general-message">Some of the tasks of this simulation require attachments. However, there are no CityBuild properties to select from. Someone needs to create properties in CityBuild before you can start this simulation.</div>

</if>
<else>
  <formtemplate id="tasks"></formtemplate>
</else>